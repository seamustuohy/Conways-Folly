#            if player_id not in players: #will only become important when colors are proceduarally genereated leter (also next if will need to have try)
#                surface.fill((255, 255, 255), the_square)

# After first round
#      if the_board[row_index][col_index] == old_board[row_index][col_index]:
#                break


            #the_screen = (col_index * sq_sz, row_index * sq_sz, sq_sz, sq_sz)
    {"A1":(2,2), "A0":(2,1), 0:(1,1), "C1":(2,3), "A1":(3,2), "B1":(3,1), 1:(3,3), "C0":(1,2), "B0":(1,3)}
